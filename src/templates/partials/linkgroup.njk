<section class="p-2 mb-2 last:mb-0 border border-gray-300 bg-gray-50 rounded-lg shadow-sm">
  <header class="text-gray-500">{{linkGroup.domain | replace(r/^www\./, '')}}</header>
  <ul>
    {% for link in linkGroup.links %}
      <li class="mb-4 last:mb-0">
        {% include "partials/link.njk" %}
      </li>
    {% endfor %}
  </ul>
</section>